[workspace]

[package]
name = "seating-gui"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
iced = { version = "0.12", features = ["svg", "image"] }
rfd = "0.15"
seating-core = { path = "../seating-core" }

[build-dependencies]
winresource = "0.1"
